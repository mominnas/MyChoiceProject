This is the React frontend that talks to the Django backend API.

Setup:
1. cd frontend
2. npm install
3. npm start

The dev server runs at http://localhost:3000 and expects the API at http://localhost:8000/api/items/